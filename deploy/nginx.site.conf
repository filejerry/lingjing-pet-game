server {
  listen 80;
  server_name your-domain.com www.your-domain.com;

  # 将所有 HTTP 请求转发到后端应用（或可重定向到 HTTPS）
  # 如要强制 HTTPS，可改为：
  # return 301 https://$host$request_uri;

  # 反向代理到 Docker 内的 app 服务（通过 docker-compose 网络名称）
  location / {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass http://127.0.0.1:14000;  # 如使用 compose 内部网络，改为 http://app:14000
    proxy_read_timeout 60s;
  }

  # 静态文件（如前端单独托管，可删除）
  location /static/ {
    root /usr/share/nginx/html;
  }
}

# 如需在 Nginx 终止 TLS（推荐生产启用），可启用下述 server 块，并使用 certbot 生成证书
# server {
#   listen 443 ssl http2;
#   server_name your-domain.com www.your-domain.com;
#   ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#   ssl_session_timeout 1d;
#   ssl_session_cache shared:MozSSL:10m;  # about 40000 sessions
#   ssl_protocols TLSv1.2 TLSv1.3;
#   ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:...;
#   ssl_prefer_server_ciphers off;
#   add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#
#   location / {
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_pass http://127.0.0.1:14000;  # 或 http://app:14000
#   }
# }