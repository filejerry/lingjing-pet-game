# 生产环境变量模板

# 应用端口（容器内部）
PORT=14000
NODE_ENV=production

# 允许的前端域名（逗号分隔）
ALLOWED_ORIGINS=https://your-frontend.com

# 数据库配置（建议使用外部托管数据库）
# SQLite（不推荐生产长期使用）：如果保留，确保 data/ 挂载持久化
# PostgreSQL 示例：
DB_CLIENT=postgres
DATABASE_URL=postgres://user:password@your-db-host:5432/spirit_pet_db

# MySQL 示例（如使用）：
# DB_CLIENT=mysql
# DATABASE_URL=mysql://user:password@your-db-host:3306/spirit_pet_db

# 日志级别
LOG_LEVEL=info