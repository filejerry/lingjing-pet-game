# 迭代03: 前端现代化架构方案

**版本**: 3.0.0
**作者**: AI开发团队
**创建日期**: 2025-10-19
**目标**: 搭建Vue3+TypeScript+Vite现代化前端架构,集成MCP协议

---

## 📋 目录

1. [技术栈选型](#技术栈选型)
2. [项目结构设计](#项目结构设计)
3. [核心架构](#核心架构)
4. [MCP协议集成方案](#mcp协议集成方案)
5. [开发路线图](#开发路线图)
6. [迁移策略](#迁移策略)

---

## 🎯 技术栈选型

### 核心框架
```json
{
  "前端框架": "Vue 3.4+ (Composition API)",
  "构建工具": "Vite 5.0+",
  "开发语言": "TypeScript 5.0+",
  "UI框架": "TailwindCSS 3.4+",
  "状态管理": "Pinia 2.1+",
  "路由": "Vue Router 4.0+",
  "HTTP客户端": "Axios 1.6+",
  "WebSocket": "Socket.IO Client 4.0+",
  "MCP客户端": "@modelcontextprotocol/sdk"
}
```

### 为什么选择Vue3?

**优势对比:**
- ✅ **学习曲线平缓**: 比React简单,适合快速迭代
- ✅ **性能优秀**: Composition API + Proxy响应式
- ✅ **体积小**: 运行时仅33KB (gzip)
- ✅ **生态完善**: Element Plus, Vant等UI库丰富
- ✅ **TypeScript原生支持**: 类型推导优秀
- ✅ **渐进式迁移**: 可与现有HTML/JS并存

---

## 📁 项目结构设计

### 新建目录结构
```
client/                          # 新前端项目根目录
├── public/                      # 静态资源
│   ├── favicon.ico
│   └── assets/                  # 图片、字体等
├── src/
│   ├── main.ts                  # 应用入口
│   ├── App.vue                  # 根组件
│   ├── router/                  # 路由配置
│   │   └── index.ts
│   ├── stores/                  # Pinia状态管理
│   │   ├── pet.ts              # 宠物状态
│   │   ├── story.ts            # 剧情状态
│   │   ├── user.ts             # 用户状态
│   │   └── battle.ts           # 战斗状态
│   ├── services/               # API服务层
│   │   ├── api.ts              # Axios配置
│   │   ├── mcp.ts              # MCP客户端
│   │   ├── petService.ts       # 宠物API
│   │   ├── storyService.ts     # 剧情API
│   │   └── battleService.ts    # 战斗API
│   ├── composables/            # 可组合函数(Vue3特性)
│   │   ├── usePet.ts           # 宠物逻辑
│   │   ├── useStory.ts         # 剧情逻辑
│   │   ├── useWebSocket.ts     # WebSocket连接
│   │   └── useMCP.ts           # MCP调用封装
│   ├── components/             # 组件
│   │   ├── common/             # 通用组件
│   │   │   ├── LoadingSpinner.vue
│   │   │   ├── ErrorBoundary.vue
│   │   │   └── ToastNotification.vue
│   │   ├── pet/                # 宠物相关
│   │   │   ├── PetCard.vue
│   │   │   ├── PetStats.vue
│   │   │   ├── PetEvolution.vue
│   │   │   └── PetChat.vue
│   │   ├── story/              # 剧情相关
│   │   │   ├── StoryDisplay.vue
│   │   │   ├── ChoiceButtons.vue
│   │   │   └── SceneAura.vue
│   │   └── battle/             # 战斗相关
│   │       ├── BattleField.vue
│   │       └── BattleLog.vue
│   ├── views/                  # 页面视图
│   │   ├── Home.vue
│   │   ├── Adventure.vue
│   │   ├── Evolution.vue
│   │   ├── Battle.vue
│   │   └── Settings.vue
│   ├── types/                  # TypeScript类型定义
│   │   ├── pet.ts
│   │   ├── story.ts
│   │   ├── battle.ts
│   │   └── mcp.ts
│   ├── utils/                  # 工具函数
│   │   ├── format.ts
│   │   ├── logger.ts
│   │   └── storage.ts
│   └── styles/                 # 样式文件
│       ├── tailwind.css
│       └── themes.css
├── index.html                   # HTML入口
├── vite.config.ts              # Vite配置
├── tsconfig.json               # TypeScript配置
├── tailwind.config.js          # TailwindCSS配置
├── package.json
└── README.md
```

---

## 🏗️ 核心架构

### 1. 应用入口 (main.ts)

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import './styles/tailwind.css'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.use(router)
app.mount('#app')
```

### 2. 状态管理架构 (Pinia)

```typescript
// stores/pet.ts
import { defineStore } from 'pinia'
import type { Pet, PetStats } from '@/types/pet'

export const usePetStore = defineStore('pet', {
  state: () => ({
    currentPet: null as Pet | null,
    petList: [] as Pet[],
    loading: false,
    error: null as string | null
  }),

  getters: {
    hasPet: (state) => state.currentPet !== null,
    petStats: (state): PetStats | null => state.currentPet?.stats || null
  },

  actions: {
    async fetchPet(petId: string) {
      this.loading = true
      try {
        const response = await petService.getPet(petId)
        this.currentPet = response.data
      } catch (error) {
        this.error = '获取宠物信息失败'
      } finally {
        this.loading = false
      }
    },

    async evolve(evolutionPath: string) {
      if (!this.currentPet) return
      const response = await petService.evolve(this.currentPet.id, evolutionPath)
      this.currentPet = response.data
    }
  }
})
```

### 3. API服务层设计

```typescript
// services/api.ts
import axios from 'axios'

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => Promise.reject(error)
)

// 响应拦截器
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // 跳转登录
      router.push('/login')
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

### 4. Composables模式 (Vue3核心特性)

```typescript
// composables/usePet.ts
import { ref, computed } from 'vue'
import { usePetStore } from '@/stores/pet'
import { useMCP } from './useMCP'

export function usePet() {
  const petStore = usePetStore()
  const { generateStory } = useMCP()
  const isEvolving = ref(false)

  const currentPet = computed(() => petStore.currentPet)
  const canEvolve = computed(() => {
    return currentPet.value && currentPet.value.exp >= 100
  })

  async function evolvePet(path: string) {
    isEvolving.value = true
    try {
      // 调用MCP生成进化故事
      const story = await generateStory({
        type: 'evolution',
        petId: currentPet.value!.id,
        evolutionPath: path
      })

      // 执行进化
      await petStore.evolve(path)

      return { success: true, story }
    } catch (error) {
      return { success: false, error: (error as Error).message }
    } finally {
      isEvolving.value = false
    }
  }

  return {
    currentPet,
    canEvolve,
    isEvolving,
    evolvePet
  }
}
```

---

## 🔌 MCP协议集成方案

### MCP架构设计

```
┌─────────────────────────────────────────────────────────┐
│                   Vue3 前端应用                           │
├─────────────────────────────────────────────────────────┤
│  Components  │  Composables  │  Stores                   │
├─────────────────────────────────────────────────────────┤
│                   MCP Client Layer                       │
│  ┌──────────────┬──────────────┬──────────────┐        │
│  │ Story Agent  │ Evolution    │ Image Gen    │        │
│  │              │ Agent        │ Agent        │        │
│  └──────────────┴──────────────┴──────────────┘        │
├─────────────────────────────────────────────────────────┤
│              HTTP/WebSocket Transport                    │
├─────────────────────────────────────────────────────────┤
│                   MCP Server (Node.js)                   │
│  ┌──────────────┬──────────────┬──────────────┐        │
│  │ AI Provider  │ Database     │ Cache        │        │
│  │ (即梦/GPT)   │ (Supabase)   │ (Redis)      │        │
│  └──────────────┴──────────────┴──────────────┘        │
└─────────────────────────────────────────────────────────┘
```

### 1. MCP客户端实现

```typescript
// services/mcp.ts
import { Client } from '@modelcontextprotocol/sdk/client/index.js'
import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js'

export class MCPClient {
  private client: Client | null = null
  private transport: StdioClientTransport | null = null

  constructor(private serverUrl: string) {}

  async connect() {
    try {
      // HTTP传输模式 (适合浏览器)
      this.client = new Client({
        name: 'lingjing-pet-game',
        version: '3.0.0',
      }, {
        capabilities: {
          sampling: {},
          tools: {}
        }
      })

      // 连接到MCP服务器
      await this.client.connect({
        type: 'http',
        url: this.serverUrl
      })

      console.log('MCP客户端连接成功')
    } catch (error) {
      console.error('MCP连接失败:', error)
      throw error
    }
  }

  async callTool(toolName: string, args: Record<string, any>) {
    if (!this.client) {
      throw new Error('MCP客户端未连接')
    }

    const response = await this.client.callTool({
      name: toolName,
      arguments: args
    })

    return response
  }

  async generateStory(params: {
    type: 'adventure' | 'daily' | 'battle' | 'evolution'
    context: Record<string, any>
  }) {
    return await this.callTool('generate_story', params)
  }

  async getEvolutionPath(petId: string, behaviors: any[]) {
    return await this.callTool('calculate_evolution', {
      petId,
      behaviors
    })
  }

  async generatePetImage(prompt: string, style: string = 'shanhaijing') {
    return await this.callTool('generate_pet_image', {
      prompt,
      style,
      provider: 'vim' // 即梦4.0
    })
  }

  disconnect() {
    this.client?.close()
  }
}

// 单例模式
export const mcpClient = new MCPClient(
  import.meta.env.VITE_MCP_SERVER_URL || 'http://localhost:3001/mcp'
)
```

### 2. MCP Composable封装

```typescript
// composables/useMCP.ts
import { ref } from 'vue'
import { mcpClient } from '@/services/mcp'
import type { StoryContext, EvolutionResult } from '@/types/mcp'

export function useMCP() {
  const loading = ref(false)
  const error = ref<string | null>(null)

  async function generateStory(context: StoryContext) {
    loading.value = true
    error.value = null

    try {
      const result = await mcpClient.generateStory({
        type: context.type,
        context: context
      })

      return result.content[0].text // MCP标准响应格式
    } catch (err) {
      error.value = (err as Error).message
      // 降级到本地模板
      return getFallbackStory(context.type)
    } finally {
      loading.value = false
    }
  }

  async function getEvolutionCandidates(petId: string, behaviors: any[]): Promise<EvolutionResult> {
    loading.value = true

    try {
      const result = await mcpClient.getEvolutionPath(petId, behaviors)
      return JSON.parse(result.content[0].text)
    } catch (err) {
      error.value = (err as Error).message
      throw err
    } finally {
      loading.value = false
    }
  }

  async function generatePetImage(prompt: string) {
    loading.value = true

    try {
      const result = await mcpClient.generatePetImage(prompt)
      return result.content[0].data // 图片URL或Base64
    } catch (err) {
      error.value = (err as Error).message
      return null // 图片失败不影响游戏
    } finally {
      loading.value = false
    }
  }

  return {
    loading,
    error,
    generateStory,
    getEvolutionCandidates,
    generatePetImage
  }
}

// 降级模板
function getFallbackStory(type: string): string {
  const templates = {
    adventure: '你的宠物开始了一段神秘的冒险...',
    daily: '平静的一天,你与宠物进行了日常互动。',
    battle: '战斗一触即发!',
    evolution: '宠物的身体开始发生奇妙的变化...'
  }
  return templates[type] || '故事继续...'
}
```

### 3. TypeScript类型定义

```typescript
// types/mcp.ts
export interface StoryContext {
  type: 'adventure' | 'daily' | 'battle' | 'evolution'
  petId?: string
  scene?: string
  action?: string
  [key: string]: any
}

export interface EvolutionResult {
  eligible: boolean
  candidates: Array<{
    path: string
    name: string
    probability: number
    requirements: string[]
  }>
  currentTendency: Record<string, number>
}

export interface MCPToolResponse {
  content: Array<{
    type: 'text' | 'image' | 'resource'
    text?: string
    data?: string
    mimeType?: string
  }>
  isError?: boolean
}
```

---

## 🚀 开发路线图

### Phase 1: 基础搭建 (第1周)

**Day 1-2: 环境配置**
- [ ] 安装Node.js 18+, pnpm
- [ ] 创建Vite+Vue3+TS项目
- [ ] 配置TailwindCSS
- [ ] 配置ESLint, Prettier

**Day 3-4: 核心架构**
- [ ] 搭建目录结构
- [ ] 配置路由和状态管理
- [ ] 封装API服务层
- [ ] 实现基础UI组件

**Day 5-7: MCP集成**
- [ ] 安装MCP SDK
- [ ] 实现MCP客户端
- [ ] 创建MCP composables
- [ ] 测试MCP连接

### Phase 2: 功能迁移 (第2周)

**Week 2: 核心功能**
- [ ] 迁移宠物系统
- [ ] 迁移剧情系统
- [ ] 迁移进化系统
- [ ] 集成WebSocket (多人对战)

### Phase 3: 优化测试 (第3周)

**Week 3: 完善细节**
- [ ] 性能优化 (代码分割、懒加载)
- [ ] 单元测试 (Vitest)
- [ ] E2E测试 (Playwright)
- [ ] 文档完善

---

## 🔄 迁移策略

### 渐进式迁移方案

```
阶段1: 双轨运行
├── /public/*.html (旧版Web页面,继续运行)
├── /client/       (新版Vue3应用)
└── 通过路由分流: /v2/* → Vue3应用

阶段2: 功能迁移
├── 优先迁移: 宠物管理、剧情系统
├── 次要迁移: 战斗、进化
└── 保留降级: API失败时回退旧版

阶段3: 完全切换
├── 新版测试通过后
├── 将/client构建为/public
└── 下线旧版HTML页面
```

### 环境变量配置

```env
# .env.development (开发环境)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_MCP_SERVER_URL=http://localhost:3001/mcp
VITE_WS_URL=ws://localhost:3000

# .env.production (生产环境)
VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_MCP_SERVER_URL=https://mcp.yourdomain.com
VITE_WS_URL=wss://api.yourdomain.com
```

---

## 📦 依赖清单

```json
{
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.7",
    "axios": "^1.6.7",
    "socket.io-client": "^4.7.4",
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.1.5",
    "typescript": "^5.3.3",
    "tailwindcss": "^3.4.1",
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "@types/node": "^20.11.24",
    "vitest": "^1.3.1",
    "playwright": "^1.42.1"
  }
}
```

---

## 🎯 成功指标

- ✅ 首屏加载 < 2秒
- ✅ MCP调用响应 < 500ms
- ✅ 代码覆盖率 > 70%
- ✅ TypeScript类型安全 100%
- ✅ 支持主流浏览器 (Chrome, Firefox, Safari)

---

## 📚 参考资料

- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Vite 官方文档](https://cn.vitejs.dev/)
- [Pinia 状态管理](https://pinia.vuejs.org/zh/)
- [MCP 协议规范](https://spec.modelcontextprotocol.io/)
- [TailwindCSS 文档](https://tailwindcss.com/docs)

---

**下一步**: 开始执行Phase 1 - 基础搭建
